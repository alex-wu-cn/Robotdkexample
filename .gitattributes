# 教学录像：三个文件都超过 GitHub 100 MB 单文件硬限制，必须走 LFS
*.mkv filter=lfs diff=lfs merge=lfs -text
*.mp4 filter=lfs diff=lfs merge=lfs -text

# RoboDK 工作站与几何体：二进制，禁止 diff 与换行转换
*.rdk binary
*.stl binary
*.step binary
*.stp binary
*.iges binary
*.robot binary

# 文本：统一 LF，保证跨平台 diff 干净
*.md text eol=lf
